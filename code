import random

def generate_message():
    greetings = ["Hello", "Hi", "Hey", "Hola", "Bonjour", "Hellooo"]
    adjectives = ["wonderful", "amazing", "fantastic", "fabulous", "spectacular", "incredible"]
    encouragements = [
        "Keep being awesome!",
        "You're doing great!",
        "Stay strong!",
        "You're a star!",
        "Keep shining!",
        "You light up the world!"
    ]
    emojis = ["😊", "🌟", "💪", "🚀", "🌈", "😄"]

    greeting = random.choice(greetings)
    adjective = random.choice(adjectives)
    encouragement = random.choice(encouragements)
    emoji = random.choice(emojis)

    message = (
        f"{greeting}, you {adjective} person! {encouragement} {emoji}"
    )
    return message

if __name__ == "__main__":
    print(generate_message())
